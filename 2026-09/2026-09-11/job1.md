# Job: 文字九州修仙 —— 境界弹窗返工（走廊对称居中 + 云层重做）

## 基本信息

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-11 |
| 项目/模块 | 文字九州修仙（godot / text-turn-game，工程名 TextTurnGame） |
| 状态 | 已完成（返工已实现、自测通过并 push：`193cb091..aa776acd`；剩真机验收，当前无 USB 设备） |
| 预估耗时 | 0.5h |
| 实际耗时 | ~0.5h（含云层重做与两次返工调试） |
| 关键字 | #文字九州修仙 #TextTurnGame #godot #境界弹窗 #卡片走廊 #云层 #对称布局 #返工 |

## 任务描述

> 承接 `2026-09-10/job5.md` 的需求①②③，按用户反馈返工两点：
> 1）卡片走廊**改回对称**，选中卡回到高度 **50%** 居中（废止 09-10 的「偏上 40% + 上下不对称缩放」）；
> 2）云层**参考天气系统里的云特效重做**：原实现太稀、且粒子向右漂移导致遮挡不住 → 改为天气系统同款「原地淡入淡出」并大幅加密。
> 约束不变：**一屏仍显示 5 张卡片**（卡片区加高 20% 的改动保留）。

## 关联工作（文件索引）

| 关系 | 文件路径 | 说明 |
|------|----------|------|
| 前序（续做来源） | `../2026-09-10/job5.md` | 需求①②③的原始实现（纵深曲线、加高 20%、初版云层）与全部需求原文 |
| 后续（续做去向） | —— | 若本批再有返工，接本文件继续 |

## 待办清单

- [x] 走廊改回对称：`ZONE_CENTER_Y_RATIO` 0.4→**0.5**（`ZONE_CENTER` = (232, 213.6)）；`SCALE_BY_DIST_UP/DOWN` 合并为单一 `SCALE_BY_DIST`，`_depth_scale(dist)` 去掉方向参数
- [x] 云层按天气系统重做：去掉初速度（原地淡入淡出，同 `weather_effect.tscn` 的 WhiteCloud：无初速、无重力、`color_ramp` 淡入淡出）；密度 6+4 → **40+18** 颗
- [x] 更新 UI 断言：对称（上下同距同缩放）、50% 居中、上下可见卡等量、云层覆盖裁切线等
- [x] 自测：`--e2e-stage-dialog` **ALL PASS (134 checks)**
- [x] 出图与视频（整窗对比、裁切线放大对比、7 秒云层动效）并发飞书
- [x] 本地提交 `aa776acd`
- [x] 用户回「继续」→ 已 push：`193cb091..aa776acd`（4 个提交），`origin/master` = `aa776acd`
- [ ] 真机验收（`bash sync_android.sh`）—— 2026-09-11 07:20 检查 `adb devices` **无设备连接**，待设备接入
- [ ] 云层浓度若需再调（太浓/太淡）：改 `color_ramp` 的 alpha 或 `amount`

## 进度记录

### 07:15 - 用户返工要求与实现

用户消息：「几个点你改一下，一是还是做成对称的吧 放在50%的位置；其次，那个云的效果，请你参考我天气系统里的那个云特效，你现在这个云的效果：一个是太稀了，第二个是你是向右移动的起不到遮挡的效果；注意要一屏显示5张卡片 这个还是不变」。

> 时间备注：机器时钟从 09-10 23:20 跳到 09-11 07:15（中途休眠），按仓库约定跨天另建本文件并与 job5 互指。

**改动**：

| 文件 | 改动 |
|------|------|
| `src/ui/stage_dialog_control.gd` | `ZONE_CENTER_Y_RATIO` 0.4→**0.5**；两张方向表合并为 `SCALE_BY_DIST := [1.0, 0.84, 0.64, 0.46, 0.34, 0.28]`；`_depth_scale(dist, above)` → `_depth_scale(dist)` |
| `src/ui/stage_cloud_layer.tscn` | ① 去掉 `direction`/`initial_velocity_*`（不再向右漂），改 `gravity = 0` + 原地淡入淡出（与 `weather_effect.tscn` 的 `WhiteCloud` 同款）；② 密度 `CloudBack` 6→**40** 颗（scale 1.0–1.4、alpha ≤0.16）、`CloudFront` 4→**18** 颗（scale 0.6–1.0、alpha ≤0.18）；③ 发射盒收窄到 (232, 18)/(220, 14)，保证每颗云都压住裁切线且不糊到标题上 |
| `src/test/ui/test_stage_dialog_100.gd` | 纵深用例改为对称版（上下同距同缩放、50% 居中、上下可见卡等量） |

**量化对比**（属性弹窗内嵌视图，云带 = 卡组区顶部 464×96 区域）：

| 指标 | 无云层 | 初版云层（09-10 23:20） | 本次重做 |
|------|--------|------------------------|----------|
| 云带亮度 p50 / p90 / max | 44 / 47 / 81 | 61 / 83 / 104 | **141 / 178 / 207** |
| 裁切线两行「被云覆盖」占比 | — | — | **亮于 70 的 100%、亮于 100 的 98%** |
| 2 秒内云带变化像素占比 | — | 约 25% | **37%~65%**（原地翻涌） |
| 选中卡位置 / 上下可见卡 | — | 40%，上 1 下 2 | **50%，上 2 下 2** |

**自测**：

| # | 项 | 结果 |
|---|----|------|
| 1 | `godot --path . --headless -- --e2e-stage-dialog` | **ALL PASS (134 checks)**（对称/50%/上下等量/云层 13 项断言） |
| 2 | 独立截图 5 张（496x569）+ 内嵌 1 张（496x898） | 重生成 |
| 3 | 云层动效视频 | 逐帧抓帧 84 张（12fps×7s）→ `stage-cloud-demo.mp4`（496x568，69KB） |

提交 **`aa776acd`**（3 files，本地**未 push**；待 push 的共 4 个：`4d5c7a8a`、`041718e4`、`ff4af050`、`aa776acd`）。

**踩坑**：`stage_cloud_layer.tscn` 里写的 `#` 注释会让 Godot 的 tscn 解析器直接报 `Parse Error`（项目里其它 tscn 都没有注释）——已删除注释，说明挪到本记录与脚本注释里。

### 07:20 - 推送远端

用户回「继续」→ 推送本批全部 4 个提交（`git fetch --prune origin` 后确认 `origin/master` = `193cb091`）：

| 项 | 值 |
|----|----|
| 推送 | `193cb091..aa776acd  master -> master`（gitee） |
| 推送后 | `origin/master` = `aa776acd`，本地已同步，工作区干净（仅遗留未跟踪 `update_code.sh`） |
| 包含提交 | `4d5c7a8a`（纵深曲线）→ `041718e4`（加高 20% + 40% 不对称，已被返工废止）→ `ff4af050`（初版云层）→ `aa776acd`（对称 50% + 云层重做） |
| 真机验收 | `adb devices` 无设备连接 → 本次跳过，待设备接入后 `bash sync_android.sh` |

## 问题与阻塞

| 问题 | 状态 | 备注 |
|------|------|------|
| 云层浓度是否合适 | 待用户真机/游戏内确认 | 当前云带亮度中位数 141（初版 61 被判「太稀」）；太浓/太淡可调 `color_ramp` 的 alpha 或 `amount` |
| 4 个提交 | 已 push | `193cb091..aa776acd`，`origin/master` = `aa776acd` |
| 真机验收 | 待设备 | `adb devices` 无设备，接入后 `bash sync_android.sh` |

## 附件（file/）

| 文件 | 说明 |
|------|------|
| `../2026-09-10/file/stage-cloud-demo.mp4` | 云层动效视频（最新版覆盖写入：原地淡入淡出 + 加密），2026-09-11 07:15 |
| `../2026-09-10/file/stage-dialog-cloud-layer-compare.png` | 整窗对比（左=上一版 / 右=本次对称+加浓），2026-09-11 07:15 |
| `../2026-09-10/file/stage-dialog-cloud-layer-zoom.png` | 裁切线放大对比（左=无云层 / 右=本次），2026-09-11 07:15 |
| `../2026-09-10/file/stage-dialog-embedded-with-cloud.png` | 本次内嵌效果（496x898），2026-09-11 07:15 |

> 附件仍归档在 2026-09-10 的 `file/`（同一批需求的素材目录），此处只做索引。

## 大文件索引（archive）

| 日期 | 存档路径 | 用途 / 说明 |
|------|----------|-------------|
| | | |

## 相关链接

- PR / Issue:
- 文档: 项目内 `README.md`、`AGENTS.md`；天气系统参考 `src/ui/weather_effect.tscn`（`WhiteCloud` 粒子参数）
- 日志/监控: 运行日志落在项目 `user://logs`

## 明日计划 / 后续跟进

- [ ] 用户确认云层浓度与动效后 push 全部 4 个提交
- [ ] 真机验收（按需 `bash sync_android.sh`）

## 变更记录

| 时间 | 变更说明 |
|------|----------|
| 2026-09-11 07:15 | 创建 job（跨天续做 job5）；实现返工：走廊对称居中 50%、云层改天气系统同款原地淡入淡出并加密（40+18 颗）；UI 断言 134 全绿；提交 `aa776acd`（未 push），出对比图/内嵌图/视频发飞书 |
| 2026-09-11 07:20 | 按用户「继续」推送远端 `193cb091..aa776acd`（4 个提交），`origin/master` = `aa776acd`；本批闭环（真机验收待设备接入） |
