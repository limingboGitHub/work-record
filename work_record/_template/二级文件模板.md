# jobN 二级文件：主题（如：验证数据 / 实测矩阵 / 根因分析 / 字段清单）

<!-- 这份文件是 job 的「折叠展开层」：job 正文只留摘要 + 链接，这里放值得日后追溯的展开内容。
     放哪里：work_record/YYYYMM/YYYYMMDD/file/jobN-<主题>.md（入 git，与 job 同一次提交；
            二级文件与附件统一放当日的 file/，文件名必须带 jobN- 前缀）
     不放什么：流水账、job 已有结论的重复、临时产物（那些进 temp/）。 -->

> 关联 job：[M/D jobN — 标题](../jobN.md)（`<code-hash>` 区间：`<起始hash>`..`<结束hash>`）
>
> 本文件要留住什么：一句话说明（如「4 条需求的实测数据与逐条验证明细，供日后复现」）

---

## 背景 / 目的

两三行说明为什么要单开这份文件（内容量、需要长期追溯、多人复用…）。

## 主体内容

<!-- 按主题分节：表格、清单、数据、分析。怎么写清楚怎么来，不用再克制篇幅。 -->

### 1. …

| 项 | 值 | 说明 |
|---|---|---|
|  |  |  |

### 2. …

## 复现方式

- 命令 / 步骤 / 环境要点（能照着再跑一遍）。
- 原始产物路径：`temp/YYYYMM/YYYYMMDD/…`（不入 git，可能已清理）、工程仓库内路径。

## 结论

- 从这些明细里能得出的结论（与 job 正文保持一致，不要另起一套说法）。